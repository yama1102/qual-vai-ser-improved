<div class="destaques neonLand my-5">
  <h1>Resultado</h1>
</div>

<div class="text-light">
  <h2>O queridinho da vez foi....</h2>
</div>
<div class="d-flex justify-content-center">
  <div class="card-trip m-5 w-50">
    <%= cl_image_tag @grouped_events[0].event.photo.key, height: 134, width: 262, crop: :fill %>
    <div class="card-trip-infos">
      <div>
        <h2><%= @grouped_events[0].event.name %></h2>
         <%= link_to "+info", event_path(@grouped_events[0].event), class: "btn btn-secondary btn-lg mt-3 mb-4 radius-15" %>
      </div>
      <h2 class="card-trip-pricing">R$ <%= @grouped_events[0].event.price %></h2>
      <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" />
    </div>
  </div>

  <div>
    <%= link_to "Voltar para Grupo", group_path(@grouped_events[0].group), class: "btn botton-back" %>
  </div>

</div>
